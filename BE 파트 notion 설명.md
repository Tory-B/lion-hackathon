# BE 파트

# BE 파트

> 최종 갱신 2026-08-11. **서울 전용 / 3레이어 / 백분위 기반 점수**로 확정됨.
> 

> 상세 문서는 프로젝트 폴더의 `레이어_점수산출_설계서.md`, `개발_킥오프_가이드.md` 참고.
> 

---

# 0. 확정된 전제

| 항목 | 확정 내용 |
| --- | --- |
| 서비스 지역 | **서울특별시 25개 자치구 한정** |
| 타겟 사용자 | 서울 예비창업자·초기 스타트업 (투자자는 확장 로드맵) |
| 레이어 | 3개 — 시장(30) / 고객(40) / 경쟁(30), 100점 만점 |
| 점수 방향 | **높을수록 안전** (리스크 낮음) |
| 점수 산출 | **백분위 기반** — 임의 구간 없음 |
| DB | PostgreSQL (로컬), 테이블 14개 |

## 왜 서울만인가

전국 확장을 데이터 기준으로 검토했고, 결론은 **불가능**이었다.

- 경기도: "발달 골목상권"만 커버, 범위 좁음
- 나머지 15개 시도: 동급 데이터 없음
- 소상공인365(전국): 데이터는 존재하나 **정식 오픈API가 원천 데이터를 안 주고 iframe URL만 제공**함. 공식 안내문에 "원천 데이터 파일을 별도로 제공하지 않습니다"라고 명시
- 결론: **원천 CSV를 정식 제공하는 건 서울시 상권분석서비스가 유일**

---

# 1. 데이터 소스 — 6종 전부 확보 완료

`db-init/data/` 에 CSV 6개, 전부 검증 통과.

| 파일 | 행수 | 쓰는 곳 | 출처 |
| --- | --- | --- | --- |
| `industry_codes.csv` | 247 | 공통 (업종 마스터) | 소상공인시장진흥공단 상가정보 |
| `store_counts_by_sgg.csv` | 6,004 | **L3** 점포수 | 상가정보 (서울만 필터) |
| `seoul_sales_quarterly.csv` | 1,323 | **L1·L2** 매출·고객 | 서울시 상권분석서비스 |
| `industry_code_mapping.csv` | 96 | 공통 (코드 연결) | 자체 제작 (1:N 매핑) |
| `industry_survival_rates.csv` | 11 | **L3** 생존율 | 국가데이터처 기업생멸행정통계 |
| `population_by_sgg.csv` | 525 | **L3** 유동인구 | 서울시 상권분석서비스 (행정동→자치구 집계) |

## 검증 결과 (2026-08-11 재확인, ALL PASS)

- 행수 6종 전부 기대값 일치
- FK 무결성 5종 전부 통과
- 서울 사업체 합계 **554,092** (원본 일치)
- 상주인구 합계 **9,360,421** (서울시 실제 인구와 일치)
- 인구·매출 데이터 분기 21개 완전 일치 (20211~20261)
- 인구 데이터 결측 0

## 인구 데이터 처리 방법

행정동 425개짜리 JSON 3개(길단위인구·상주인구·직장인구)를 받아서, `adstrd_cd` 앞 5자리로 **자치구 25개에 집계**했다. 행정동이 자치구보다 작은 단위라 합치는 게 가능하다 (반대는 불가능).

## 알아야 할 데이터 한계

| 한계 | 내용 |
| --- | --- |
| 업종 매핑 커버리지 | 247개 소분류 중 **96개만** 서울 매출데이터와 매핑됨. 나머지는 L1·L2가 NULL |
| 자치구별 매출 없음 | 매출 데이터는 **서울시 전체 × 업종** 단위. "중랑구 카페 매출"은 구할 수 없음 → 자치구별 침투율 계산 불가 |
| 상주인구 갱신 주기 | **연 1회.** 4분기 값이 다음 해 1~3분기에 반복되므로 성장률 계산 금지 |
| 유동인구 중복 | 분기 누적치이며 같은 사람이 여러 동에서 잡힘. 절대값보다 **자치구 간 상대 비교**로만 사용 |
| L3 표본 부족 | 6,004개 조합 중 **23.2%가 점포수 10개 미만** → LOW_SAMPLE 처리 필요 |

## 확보 못 한 것

| 데이터 | 상태 |
| --- | --- |
| 소규모상가 임대료 | `data.go.kr/data/15069766` (한국부동산원). CSV 제공 확인, 자치구 단위인지는 미확인 |
| 개·폐업 추이 | 쓸 만한 공개 데이터 못 찾음 |

둘 다 없어도 3개 레이어 점수가 전부 산출되므로 개발을 막지 않는다.

---

# 2. 점수 계산 방식 — 백분위

## 핵심 원칙

```
지표 점수 = (서울 분포에서의 백분위 순위 / 100) × 지표 배점
```

CAGR이 63개 업종 중 상위 20%(백분위 80)면 → 15점 × 0.80 = **12.0점**

**임의로 정한 구간이 하나도 없다.** "CAGR 3.4%가 왜 경계인가?"라는 질문 자체가 사라지고, 경계에서 점수가 뚝 끊기는 문제도 없다.

## 레이어별 지표와 배점

| 레이어 | 배점 | 지표 | 배점 | 방향 |
| --- | --- | --- | --- | --- |
| **L1 시장** | 30 | 시장 규모 (최근 4분기 매출 합) | 10 | 높을수록 ↑ |
|  |  | 5년 CAGR | 15 | 높을수록 ↑ |
|  |  | 최근 모멘텀 (최근1년 − CAGR) | 5 | 높을수록 ↑ |
| **L2 고객** | 40 | 성별 분산 | 5 | 쏠릴수록 ↓ |
|  |  | 연령 분산 | 5 | 쏠릴수록 ↓ |
|  |  | 실수요 증가율 (매출**건수** CAGR) | 8 | 높을수록 ↑ |
|  |  | 소비패턴 안정성 (주말비중) | 10 | 중앙값에서 멀수록 ↓ |
|  |  | **정성 검증 (초기 0)** | **12** | 검증으로만 채움 |
| **L3 경쟁** | 30 | **실질 경쟁 강도 (점포당 유동인구)** | 15 | 높을수록 ↑ |
|  |  | 업종 5년 생존율 | 15 | 높을수록 ↑ |

## L2 데이터 상한 28점 — 의도된 설계

초기 진단에서 L2는 아무리 잘해도 **28/40**이다. 자동으로 "고객 검증 부족"이 뜬다. 이건 버그가 아니라 서비스 정체성이 점수 구조에 박힌 것 — 데이터로는 "누가 사는지"까지만 알 수 있고 "왜 사는지"는 사람한테 물어야 한다. 나머지 12점은 질문지 → 검증 결과 입력으로만 채워진다.

## 폐기된 지표 (기록용)

**객단가(매출÷건수)를 점수화하려다 폐기했다.** 실제로 돌려보니 카페가 8,177원으로 최하점을 받았는데, 이건 카페가 원래 객단가 낮은 업종인 거지 리스크가 아니다. 업종 고유 특성을 리스크로 오인하는 지표라 버리고 **매출건수 CAGR(실수요 증가율)**로 교체했다. 객단가는 점수화하지 않고 근거 정보로만 표시한다.

---

# 3. 등급 — 실제 분포의 3분위

2,395개 조합(매핑 가능 업종 × 25개 구)을 전부 계산해서 나온 분포의 P33/P67을 경계로 사용.

| 레이어 | 위험 (HIGH) | 보통 (MEDIUM) | 안전 (LOW) |
| --- | --- | --- | --- |
| L1 시장 (30점) | < 15.6 | 15.6 ~ 21.6 | ≥ 21.6 |
| L2 고객 (28점 기준) | < 13.4 | 13.4 ~ 17.6 | ≥ 17.6 |
| L3 경쟁 (30점) | < 9.2 | 9.2 ~ 14.5 | ≥ 14.5 |
| **총점 (88점 기준)** | **< 41.9** | **41.9 ~ 50.3** | **≥ 50.3** |

적용 결과 **안전 33.0% / 보통 33.9% / 위험 33.0%** — 거의 정확히 1/3씩 갈린다. 등급이 변별력을 갖는다는 뜻.

> 이 등급은 **"서울 안에서의 상대적 위치"**다. "안전 = 절대적으로 안전"이 아니라 "다른 업종·지역 조합과 비교해 상위 1/3"이라는 의미. 화면에도 이렇게 표기할 것.
> 

## 실제 계산 검증

| 케이스 | L1 | L2(데이터) | L3 | 총점/88 |
| --- | --- | --- | --- | --- |
| 카페 / 중랑구 (540개) | 22.3 안전 | 22.2 안전 | 12.3 보통 | **56.8 안전** |
| 카페 / 종로구 (1,248개) | 22.3 안전 | 22.2 안전 | 4.4 위험 | **48.9 보통** |
| 카페 / 강남구 (2,123개) | 22.3 안전 | 22.2 안전 | 2.4 위험 | **46.9 보통** |

최상위: 피부/비뇨기과 의원 / 도봉구 (73.5) · 최하위: 기숙사·고시원 / 동작구 (9.2)

## 점포당 유동인구가 왜 더 정확한가

기존 "점포수 ÷ 서울평균"은 그 지역에 사람이 얼마나 다니는지를 무시했다. 인구 데이터를 붙이니 순위가 이렇게 바뀐다 (카페 기준):

| 자치구 | 점포수 | 단순 밀도 | 점포당 유동인구 |
| --- | --- | --- | --- |
| 강남구 | 2,123 | 25위 | **20위** ↑ |
| 중구 | 1,133 | 19위 | **25위** ↓ |
| 도봉구 | 389 | 1위 | **4위** ↓ |

"점포가 적으니 좋다"가 아니라 **"손님 대비 경쟁자가 적으니 좋다"**로 바뀐 것.

---

# 4. DB 테이블 (14개)

## [A] 공공데이터 참조 (읽기 전용, CSV 1회 적재)

| 테이블 | 행수 | 비고 |
| --- | --- | --- |
| `industry_codes` | 247 | PK: small_code |
| `store_counts_by_sgg` | 6,004 | UK: (small_code, sgg_code) |
| `seoul_sales_quarterly` | 1,323 | UK: (industry_code, quarter) |
| `industry_code_mapping` | 96 | UK: small_code |
| `industry_survival_rates` | 11 | UK: large_code |
| **`population_by_sgg`** | **525** | **신규.** UK: (sgg_code, quarter) |

## [B] 서비스 운영 (JPA 관리)

`analysis_requests` · `diagnosis_results` · `layer_evidences` · `unverified_hypotheses` · `questionnaires` · `questionnaire_items` · `verification_results` · `customer_score_history`

**주요 제약**

- `analysis_requests.region_sgg_code` — **NOT NULL + CHECK(`^11[0-9]{3}$`)**. 서울 자치구 코드만 허용
- `diagnosis_results.market_score` / `customer_score` — **NULL 허용**. 업종 매핑 실패 시 NULL

---

# 5. 개발 현황

| 단계 | 상태 |
| --- | --- |
| Step 1 · DB 세팅 | 스크립트 준비 완료 (`db-init/schema.sql`, `load_data.sql`) |
| Step 2 · 공공데이터 엔티티 | **완료** (커밋 `feat/db-entities`) |
| Step 3 · 서비스 운영 엔티티 | **완료** (커밋 `feat/service-entities`) |
| Step 4 · 공통 응답·예외 처리 | 다음 |
| Step 5~7 · 분석요청·진단·근거 API | **데모 핵심** |
| Step 8~10 · 질문지·검증·이력 | 시간 남으면 |

## 지금 당장 해야 할 것 2가지

1. **`PopulationBySgg` 엔티티·리포지토리 추가** — 테이블은 만들었는데 엔티티가 아직 없다
2. **점수 필드 타입 변경 검토** — 현재 `DiagnosisResult`의 점수가 전부 `Integer`인데, 백분위 기반이라 **22.3, 56.8 같은 소수가 나온다.** 반올림해서 Integer로 갈지, `BigDecimal`로 바꿀지 정해야 함

---

# 6. 구현 시 주의사항

1. **백분위 테이블은 앱 기동 시 1회 계산해서 캐싱.** 요청마다 재정렬하면 느리다. 데이터가 정적이므로 `@PostConstruct`에서 한 번만.
2. **백분위 계산은 이진탐색(`Collections.binarySearch`)으로.**
3. **모든 지표를 `layer_evidences`에 개별 행으로 저장.** 점수만 저장하면 "왜 이 점수인지" 화면에 못 보여준다. **원본 실제값("CAGR +4.0%")과 백분위("서울 상위 23%")를 둘 다** 남길 것.
4. **비율 지표엔 분모를 항상 같이 저장.** L3는 점포수 n을 기록해야 LOW_SAMPLE 판정 가능.
5. **등급은 저장하지 말고 조회 시 계산.** 경계값이 바뀌면 과거 데이터도 자동으로 새 기준 적용.
6. **NULL을 100점 만점으로 환산하지 말 것.** 매핑 실패 시 30점 만점에 24점을 "80점"으로 바꾸면, 시장·고객을 전혀 모르는데 안전해 보이는 오해가 생긴다.

---

# 7. LLM 사용 범위 (OpenAI)

**쓰는 곳**

- 아이템 자연어 → 업종코드 매핑 (`matched_code`, `match_accuracy`)
- 미검증 가설 생성 (`unverified_hypotheses`) — 반드시 "AI 추정·검증 필요" 라벨과 함께
- 검증 질문지 문항 생성 — 유도질문 배제, 과거 경험·실제 지출 중심

**쓰면 안 되는 곳**

- **L1·L2·L3의 숫자를 LLM으로 만들어내는 것.** 없는 데이터를 그럴듯하게 지어내는 것이라 서비스 정체성과 정면으로 충돌. 데이터 없으면 NULL로 두고 "확인 불가"로 표시할 것

API 키는 코드에 쓰지 말고 `.env` 환경변수로 (`.gitignore`에 포함 확인됨).